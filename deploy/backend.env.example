# Copy this file on the server to /etc/testproject/backend.env.
# Never commit or paste the real password into the repository or chat.

DB_URL=jdbc:postgresql://127.0.0.1:5432/testproject
DB_USERNAME=testProject
DB_PASSWORD=replace-with-a-new-database-password

MANAGE_USERNAME=admin
MANAGE_PASSWORD=replace-with-a-long-random-admin-password
